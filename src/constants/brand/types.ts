export interface BrandConfig {
  key: string // 自身識別用
  name: string // 中文名稱
  title: string // 網站標題
  logo: string // logo 圖片路徑
  primaryColor: string // 品牌主色
  publicPath: string // 網站根路徑（如 /xiangchuan）
}
